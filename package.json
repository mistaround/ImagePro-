{
  "name": "imagepro",
  "version": "0.1.0",
  "description": "跨平台桌面端图片对比工具",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite build --config vite.electron.config.ts && concurrently -n renderer,electron \"vite\" \"sleep 3 && electron . --dev\"",
    "build": "vite build && vite build --config vite.electron.config.ts && electron-builder --dir",
    "build:exe": "vite build && vite build --config vite.electron.config.ts && electron-builder",
    "preview": "vite build && vite build --config vite.electron.config.ts && electron .",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.electron.json"
  },
  "keywords": ["image", "comparison", "AIGC"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sharp": "^0.33.5",
    "zustand": "^4.5.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.28",
    "@types/react-dom": "^18.3.7",
    "@vitejs/plugin-react": "^4.7.0",
    "concurrently": "^8.2.2",
    "electron": "^30.5.1",
    "electron-builder": "^24.13.3",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "wait-on": "^7.2.0"
  }
}
